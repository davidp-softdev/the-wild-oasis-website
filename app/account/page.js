import Navigation from "../_components/Navigation";

export const metadata = {
  title: "Account",
};

export default function Page() {
  return (
    <div>
      <h1>Account page</h1>;
    </div>
  );
}
